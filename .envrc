# Load environment files if they exist
[[ -f ~/.config/payready/env.base ]] && dotenv ~/.config/payready/env.base
[[ -f ~/.config/payready/env.llm ]] && dotenv ~/.config/payready/env.llm
[[ -f ~/.config/payready/env.rag ]] && dotenv ~/.config/payready/env.rag
[[ -f ~/.config/payready/env.agno ]] && dotenv ~/.config/payready/env.agno
[[ -f ~/.config/payready/env.jobs ]] && dotenv ~/.config/payready/env.jobs
[[ -f ~/.config/payready/env.biz ]] && dotenv ~/.config/payready/env.biz
[[ -f ~/.config/payready/env.platform ]] && dotenv ~/.config/payready/env.platform
[[ -f ~/.config/payready/env.github ]] && dotenv ~/.config/payready/env.github

# Load date context
eval $(./bin/get-date-context.sh 2>/dev/null || true)
export PATH="$PWD/bin:$PATH"